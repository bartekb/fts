indexer
{
}

searchd
{
  listen = 127.0.0.1:9306:mysql41
  log = /home/xef/Projekty/fts/log/development.searchd.log
  query_log = /home/xef/Projekty/fts/log/development.searchd.query.log
  pid_file = /home/xef/Projekty/fts/log/development.sphinx.pid
  workers = threads
  binlog_path = /home/xef/Projekty/fts/tmp/binlog/development
}

source document_core_0
{
  type = pgsql
  sql_host = localhost
  sql_user = buba
  sql_pass = 123qwe
  sql_db = fts_development
  sql_port = 5432
  sql_query_pre = SET TIME ZONE UTC
  sql_query = SELECT "documents"."id" * 3 + 0 AS "id", "documents"."title" AS "title", "documents"."body" AS "body", categories."title" AS "category", "documents"."id" AS "sphinx_internal_id", 'Document' AS "sphinx_internal_class", 0 AS "sphinx_deleted", "documents"."category_id" AS "category_id", extract(epoch from "documents"."created_at")::int AS "created_at", extract(epoch from "documents"."updated_at")::int AS "updated_at", RADIANS(latitude) AS "latitude", RADIANS(longitude) AS "longitude" FROM "documents" LEFT OUTER JOIN "categories" ON "categories"."id" = "documents"."category_id" WHERE ("documents"."id" BETWEEN $start AND $end) GROUP BY "documents"."id", "documents"."title", "documents"."body", categories."title", "documents"."id", "documents"."category_id", "documents"."created_at", "documents"."updated_at", latitude, longitude
  sql_query_range = SELECT COALESCE(MIN("documents"."id"), 1), COALESCE(MAX("documents"."id"), 1) FROM "documents"
  sql_attr_uint = sphinx_internal_id
  sql_attr_uint = sphinx_deleted
  sql_attr_uint = category_id
  sql_attr_timestamp = created_at
  sql_attr_timestamp = updated_at
  sql_attr_float = latitude
  sql_attr_float = longitude
  sql_attr_string = sphinx_internal_class
  sql_field_string = title
  sql_field_string = category
  sql_query_post_index = UPDATE "documents" SET "delta" = FALSE WHERE "delta" = TRUE
  sql_query_info = SELECT "documents".* FROM "documents"  WHERE ("documents"."id" = ($id - 0) / 3)
}

index document_core
{
  type = plain
  path = /home/xef/Projekty/fts/db/sphinx/development/document_core
  docinfo = extern
  charset_type = utf-8
  source = document_core_0
}

source document_delta_0
{
  type = pgsql
  sql_host = localhost
  sql_user = buba
  sql_pass = 123qwe
  sql_db = fts_development
  sql_port = 5432
  sql_query_pre = SET TIME ZONE UTC
  sql_query = SELECT "documents"."id" * 3 + 0 AS "id", "documents"."title" AS "title", "documents"."body" AS "body", categories."title" AS "category", "documents"."id" AS "sphinx_internal_id", 'Document' AS "sphinx_internal_class", 0 AS "sphinx_deleted", "documents"."category_id" AS "category_id", extract(epoch from "documents"."created_at")::int AS "created_at", extract(epoch from "documents"."updated_at")::int AS "updated_at", RADIANS(latitude) AS "latitude", RADIANS(longitude) AS "longitude" FROM "documents" LEFT OUTER JOIN "categories" ON "categories"."id" = "documents"."category_id" WHERE ("documents"."delta" = TRUE AND "documents"."id" BETWEEN $start AND $end) GROUP BY "documents"."id", "documents"."title", "documents"."body", categories."title", "documents"."id", "documents"."category_id", "documents"."created_at", "documents"."updated_at", latitude, longitude
  sql_query_range = SELECT COALESCE(MIN("documents"."id"), 1), COALESCE(MAX("documents"."id"), 1) FROM "documents"  WHERE ("documents"."delta" = TRUE)
  sql_attr_uint = sphinx_internal_id
  sql_attr_uint = sphinx_deleted
  sql_attr_uint = category_id
  sql_attr_timestamp = created_at
  sql_attr_timestamp = updated_at
  sql_attr_float = latitude
  sql_attr_float = longitude
  sql_attr_string = sphinx_internal_class
  sql_field_string = title
  sql_field_string = category
  sql_query_info = SELECT "documents".* FROM "documents"  WHERE ("documents"."id" = ($id - 0) / 3)
}

index document_delta
{
  type = plain
  path = /home/xef/Projekty/fts/db/sphinx/development/document_delta
  docinfo = extern
  charset_type = utf-8
  source = document_delta_0
}

index document
{
  type = distributed
  local = document_core
  local = document_delta
}
